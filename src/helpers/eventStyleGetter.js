export const eventStyleGetter = (e, start, end, isSelected) => {
  const style = {
    backgroundColor: "#000",
    borderRadius: "5px",
    opacity: 0.6,
    color: "#00f450",
  };
  return { style };
};
